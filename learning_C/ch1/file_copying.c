# include <stdio.h>

/*
int main () {
    int c;
    c = getchar();
    printf("%d", c);
    while (c!= EOF) {
        putchar(c);
        c = getchar();
    }
}
*/

// assignments evaluate to the RHS

int main() {
    printf("%d", EOF); // EOF = -1
}